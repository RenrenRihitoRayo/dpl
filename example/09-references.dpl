&use {std/text_io.py}

dict person
    set name = "Darren"
end

fn test(reference, new_val)
    setref :reference = :new_val
end

io:println(:person.name)

test(person.name::ref, Andrew)

io:println(:person.name)

test(person.name::ref, Carl)

io:println(:person.name)