&use {std/text_io.py}

io:input "Name: " name

set loged_in [Jharred David Noah]

match :name
    with "admin"
        io:println "Logged in as admin"
        ...
    end
    as user
    case (:name in :loged_in)
        io:println 'Welcome ${user}'
    end
end