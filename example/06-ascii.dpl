use {std/text_io.py}

set start = [from_ascii "A"]
set end   = [[from_ascii "Z"] + 1]

for i in [drange :start :end]
    io:print([to_ascii :i])
end

io:println()

set start = [from_ascii "0"]
set end   = [[from_ascii "9"] + 1]

for i in [drange :start :end]
    io:print([to_ascii :i])
end

io:println()