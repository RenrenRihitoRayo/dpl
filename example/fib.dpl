&use {std/text_io.py}

fn fib(n)
    set a = 1
    set b = 0
    set c = 0
    while (:n > 0)
        set c = [:a + :b]
        set a = :b
        set b = :c
        set n = [:n - 1]
    end
    return :c
end

catch (result) fib 10
