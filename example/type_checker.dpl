&use {std/text_io.py}

set _meta.debug.type_checker true
# complain about missing type signatures
set _meta.debug.TC_DEFAULT_WHEN_NOT_FOUND false

io:println "Test"

# intentionally do not register
# tc_register "greet :: str"
fn greet name
    io:println [fast-format "Hello ${name}!"]
end

greet John
greet 90