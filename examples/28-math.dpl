&use {std/text_io.py}

# yes actual operations with precedence
# supports arithmetic, and, or, and not
# as well as comparisons
# explicit though
set res = [. 1 + 8 * 2]

assert [:res != 17], "result is unexpected"

io:println('\e[33mResult: ${:res}\e[0m')
